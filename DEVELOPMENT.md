# 開発記録

要件書 `ai-phone-agent-spec.md`(AI電話一次応答システム)の検証実装。2026-08-12 に一気に組んだ。
この文書は「何を作ったか」ではなく「なぜそうしたか」と「何を踏んだか」を残す。

## 構成の決定

```
発信者 ── (ブラウザ / Twilio Media Streams) ── WebSocket ── server.mjs
                                                    ├─ stt.mjs      Google Speech-to-Text v2 (streaming)
                                                    ├─ dialog.mjs   会話ステートマシン(進行の全権)
                                                    ├─ claude.mjs   分類・応答文・要約(claude-haiku-4-5)
                                                    ├─ tts.mjs      Google Text-to-Speech (Chirp3-HD)
                                                    ├─ classroom.mjs 教室データ・学年正規化・空き枠判定
                                                    └─ notify.mjs   SMS/メール通知・カレンダー仮予約
```

- **進行はステートマシン、LLMは分類と文面だけ**(§7.4)。LLMに進行を渡すと通話が迷子になるため。
- **音声の入口は2つ**(ブラウザ=16kHz LINEAR16 / 電話=8kHz μ-law)だが、会話より下は完全に共通。
  Twilio に差し替えるときに会話側の作り直しが発生しない。
- **8kHz μ-law は変換せずそのまま Google に流す**。両方が対応しており、§7.2 も満たせる。
- **定型文は起動時に事前合成**(§7.1)。教室データから決まる文(空き枠の案内、日時確認、
  在籍生徒向けの冒頭文)も全列挙して作り置きする。実測でこれが応答遅延の主因だった。

## 検証の道具

| コマンド | 役割 |
|---|---|
| `npm run phone-agent:replay` | 台本21本をテキストで流して採点。要件書の条項ごとに期待値を書く |
| `npm run phone-agent:sim` | Twilio の通信手順(connected→start→media→mark→stop)を模して通話1本 |
| `npm run phone-agent:check` | 合成→認識の疎通確認(マイク不要) |
| `scripts/voice-samples.mjs` | 声と文言の聴き比べサンプル生成 |

台本の採点は `neverSays` / `firstReplyIncludes` で「正しい理由で合格しているか」まで見る。
最終結果だけ見ると、途中で仕様違反していても通ってしまう。
LLM分類が絡むため採点は実行ごとに1本程度ぶれる。1回の結果で判断しない。

## 踏んだバグと対処(時系列)

1. **STTの発話が二重確定・途中分割** — 無音タイマー1本で切っていた。認識確定(isFinal)基準に変え、
   1発話ごとにストリームを張り直す方式へ。Google は確定済みテキストを同一ストリームで再送してくることがある。
2. **「小学」→「同額」誤認識**(合成音声でのループバック時)。語彙ヒント(phraseSets)を追加。
   8kHz では「小5」→「兵庫」も出たため、学年語彙(小1〜中3・年長)も追加。
3. **AI発話中にSTTストリームがタイムアウト**(`Stream timed out`)。喋っている間は音声を送らないため。
   音声が来た時点で開き直す遅延オープンに変更。これで平均遅延 2.3秒→1.1秒。
4. **事実ガードの誤判定** — 月謝 8250 を Claude が「8,250円」と書いただけで「事実に無い数値」と
   誤判定し正答を破棄。桁区切りカンマを除去してから数値比較。※同じバグが `src/lib/chat-ai.ts` にもある。
5. **事実に無い否定の捏造** — 「近隣にコインパーキングあり」から「教室に駐車場はございません」と断定。
   プロンプトでは確率的に防げず、否定表現の機械検査を追加。
6. **静的配信で存在しないファイルを踏むとプロセスごと落ちる** — 200ヘッダを先に書いていた。
   ブラウザの /favicon.ico 自動取得が引き金。open イベント後にヘッダを書く。
7. **ポート衝突で ws 経由の未処理エラー落ち** — ws は HTTP サーバのエラーを WebSocketServer にも転送する。
   受け手を置き、空きポートへ自動退避。
8. **「中学2年生」への訂正を無視**(スクリーンショット報告)。枠選び状態では曜日しか見ていなかった。
   言い直し(学年・コース・枠・氏名・体験⇄見学)を route() の頭で一括受け付けに。
9. **LLMが「中学2年生」を「2年生」に丸めて抽出** — 学年は発話の字面を優先(`gradeFrom`)。
10. **見学が途中で体験に巻き戻る** — 「はい」等にLLMが visitType=lesson を出す。字面に「体験」が
    あるときだけ巻き戻すよう変更。確率に頼る判定は必ず字面のバックストップを置くこと。
11. **電話番号の聞き取りは11桁言わせると壊れる** — 発信者番号が取れていれば「おかけの番号でよいか」の
    確認に切り替え(§7.3)。誤認識時も発信者番号にフォールバック。

12. **busy中の発話を捨てていた** — AIが応答を考えている間に届いた発話を無視しており、
    長い発話がSTTで2つに割れると後半だけ処理されて学年などが消えた。キューして順に処理。
13. **寄り添い(共感)の欠如** — 「迷ってるんだよね」に即「何年生ですか」と返していた。
    分類に feeling(迷い/不安)を追加し、気持ちの種類ごとに1回、固定の一言を質問の前に添える。
    事実(体験無料)は notes に書かれている場合だけ口にする。一言+質問の組も事前合成する。
14. **バージイン(会話被せ)対応** — AI発話中の声を受け付ける。電話経路は Twilio の clear で
    即座に黙る(受話音声に自声は混ざらない)。ブラウザ経路は再生停止+自声エコーの字面判定で
    誤反応を抑える。

15. **定型文の連結が「しっくりこない」**(スクリーンショット報告)。決まった文を機械的につなぐと、
    文脈に合わない相づちが出たり硬くなったりする。「何を言うか」はステートマシン、
    **「どう言うか」だけ LLM** に任せる層(`rephrase`)を追加。元の文に無い数値・否定は事実ガードで弾く。
    毎回作り直すと遅く・揺れるので、質問された/気持ちが出た/こみ入った発話のときだけ発動する。
16. **共感の前置きが的外れ** — 「話だけ聞きたい」に「体験は無料です」を出していた。固定文の前置きを
    廃止し、`rephrase` に「添えてよい事実(notes)」を渡して文脈に合うものだけ選ばせる形へ。
17. **ごきょうだいの同時申込で1人目が消える** — 「小2の子もいるので」を学年の言い直しと誤認していた。
    LLMの siblingGrade に加え「〜の子もいる」等の字面を併用し、両方を記録して日時は先生に調整させる。

## 設計判断のメモ

- **仮予約は「取れない理由」があっても取る**。時間割に無い日時・満席・要相談はすべて
  `needsConsult` の仮予約として希望の発話ごと預かり、先生が折り返しで調整する。
  電話を切らせたら見込み客ごと失う。
- **notes(先生の承認済み案内事項)** が柔軟性の中心。§3.4 の定型範囲外でも、notes に書かれていれば
  「先生の代読」として回答してよい。答えが無ければ NOANSWER を返させて先生へ渡す。
  事実ガード(数値・否定捏造)は notes 回答にもかかる。
- **在籍生徒の登録番号** からの着信は伝言モードで受ける。冒頭で「先生にお伝えします」と言い切り、
  予約フローに引き込まない。判定は下10桁一致。
- **声** は Chirp3-HD。`telephony-class-application` フィルタは留守電の音になるので使わない
  (電話経由はどのみち8kHzに落ちる)。Chirp3-HD は speakingRate を無視する(§7.2 未達、起動時に警告)。
- **言い方(rephrase)と内容(ステートマシン)を分ける**。自然さは言い方の問題であって、
  進行をLLMに渡す理由にはならない(§7.4)。ただし言い回しは確率的になるため、
  台本の判定は語句一致ではなく「予約に到達したか」等の結果で見る。
- **話し終わりの判定** は3層: Google の speechEndTimeout(1.2s) + 確定後の待ち(0.9s) +
  日本語の継続語尾(「〜ですけど」等)でさらに +0.8s。食い込むより待つほうを取った。

## 未解決・次の課題

- **電話回線**: Google に日本の番号払い出しAPIは無い。Twilio の日本050は法人審査(登記簿+郵送)。
  米国番号なら即日だが、かける側が国際通話料(au 1分78円)を負担し、SUBLINE からの転送先にできない。
- **Twilio無料トライアルの制約**: 英語の前置きアナウンス+認証済み番号からのみ着信。実演には $20 チャージが必要。
- **氏名・電話番号の復唱は事前合成できない**ため、そこだけ §1.3 の1.5秒を超える(合成1.4〜2.2秒)。
- `src/lib/chat-ai.ts` のカンマ誤判定(上記4)は本体側に残っている。

## 実測値(2026-08-12 時点)

- 台本採点: 23本中 22〜23(言い回しが確率的なため実行ごとに±1本ぶれる)
- 応答遅延(電話経路シミュレーション): 平均 約1.1秒、6/7 が1.5秒以内
- 通話1本(3分)の推定原価: 約18円(STT $0.048 + TTS $0.018 + Claude $0.009 + Twilio $0.043)
